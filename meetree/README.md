# MeeTree

The canonical README is maintained at the repository root:

```text
../README.md
```

Release archives replace this pointer with a copy of the root README, so the packaged Nextcloud app still contains the full documentation.
